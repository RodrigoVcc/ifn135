{
  "name": "sistema",
  "private": true,
  "version": "1.0.0",
  "description": "Sistema de pedidos, inventario y producción",
  "scripts": {
    "dev": "concurrently \"pnpm --filter api dev\" \"pnpm --filter web dev\"",
    "build": "pnpm --filter web build",
    "api": "pnpm --filter api dev",
    "web": "pnpm --filter web dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
